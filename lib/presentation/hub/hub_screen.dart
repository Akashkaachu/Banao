import 'package:flutter/material.dart';

class HubScreenPage extends StatelessWidget {
  const HubScreenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
